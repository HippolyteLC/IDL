### Task 1.1

The classifier has no pair of centres which is very close to another centre. Therefore, it should perform decently well. There seems to be three pairs of numbers whose centres are positioned close: 7 and 9, 8 and 9, and 5 ad 9.

### Task 1.2

The different dim-reduction algorithms produce somwhat distinct clusters, especially the t-SNE and UMAP plots. I ran the three algorithms with their default parameters, so their are differences attributable to that. Otherwise, methods such as UMAP are good at producing distinct, tight clusters when are different classes.(with default parameters).